
name: team_repository

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.14.4
  rxdart: ^0.25.0
  equatable: ^1.2.5
  firebase_core: ^0.5.3 #^0.4.0+7
  firebase_auth: ^0.18.2
